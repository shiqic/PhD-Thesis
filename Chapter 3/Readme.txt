The python script "psrot_committor" calculates the committor for a given set of structures.
One needs to set the criteria (e.g. a distance threshold) to judge whether the simulation first reaches one of the stable structures (states).
